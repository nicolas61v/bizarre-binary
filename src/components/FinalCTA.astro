---
/* FinalCTA — Bold full-width call to action */
---

<section class="cta" id="contacto">
    <div class="cta__bg">
        <div class="cta__glow cta__glow--1"></div>
        <div class="cta__glow cta__glow--2"></div>
        <div class="cta__line cta__line--1"></div>
        <div class="cta__line cta__line--2"></div>
        <div class="cta__line cta__line--3"></div>
    </div>

    <div class="cta__inner container reveal">
        <p class="cta__label">¿Lista para comenzar?</p>

        <h2 class="cta__title">
            Tu momento <br />
            <span class="gold-gradient-text">es ahora</span>
        </h2>

        <p class="cta__text">
            Únete a las más de 50 modelos que ya están generando ingresos
            reales con Dejavu Estudios. Sin experiencia previa requerida.
        </p>

        <div class="cta__actions">
            <a
                href="https://wa.me/573000000000"
                target="_blank"
                rel="noopener"
                class="cta__btn cta__btn--primary"
            >
                <svg
                    width="20"
                    height="20"
                    viewBox="0 0 24 24"
                    fill="currentColor"
                >
                    <path
                        d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347z"
                    ></path>
                    <path
                        d="M12 0C5.373 0 0 5.373 0 12c0 2.625.846 5.059 2.284 7.034L.789 23.492a.5.5 0 00.611.611l4.458-1.495A11.94 11.94 0 0012 24c6.627 0 12-5.373 12-12S18.627 0 12 0zm0 22c-2.287 0-4.4-.758-6.103-2.037l-.426-.323-2.648.888.888-2.648-.323-.426A9.96 9.96 0 012 12C2 6.486 6.486 2 12 2s10 4.486 10 10-4.486 10-10 10z"
                    ></path>
                </svg>
                <span>WhatsApp</span>
            </a>
            <a href="#servicios" class="cta__btn cta__btn--secondary">
                Saber Más
            </a>
        </div>

        <p class="cta__disclaimer">
            ✦ Proceso 100% confidencial · Resultados desde el primer mes
        </p>
    </div>
</section>

<style>
    .cta {
        position: relative;
        padding: clamp(5rem, 12vw, 10rem) 0;
        overflow: hidden;
        text-align: center;
    }

    /* Background effects */
    .cta__bg {
        position: absolute;
        inset: 0;
        pointer-events: none;
    }

    .cta__glow {
        position: absolute;
        border-radius: 50%;
        filter: blur(100px);
    }

    .cta__glow--1 {
        width: 500px;
        height: 500px;
        top: -20%;
        left: 50%;
        transform: translateX(-50%);
        background: radial-gradient(
            circle,
            rgba(212, 175, 55, 0.1) 0%,
            transparent 70%
        );
    }

    .cta__glow--2 {
        width: 300px;
        height: 300px;
        bottom: -10%;
        right: 10%;
        background: radial-gradient(
            circle,
            rgba(212, 175, 55, 0.06) 0%,
            transparent 70%
        );
    }

    /* Animated horizontal lines */
    .cta__line {
        position: absolute;
        height: 1px;
        background: linear-gradient(
            90deg,
            transparent,
            rgba(212, 175, 55, 0.15),
            transparent
        );
        animation: ctaLineSweep 8s ease-in-out infinite;
    }

    .cta__line--1 {
        top: 15%;
        left: -20%;
        width: 140%;
        animation-delay: 0s;
    }
    .cta__line--2 {
        top: 50%;
        left: -20%;
        width: 140%;
        animation-delay: 2.5s;
        opacity: 0.5;
    }
    .cta__line--3 {
        top: 85%;
        left: -20%;
        width: 140%;
        animation-delay: 5s;
        opacity: 0.3;
    }

    @keyframes ctaLineSweep {
        0%,
        100% {
            transform: translateX(-30%);
            opacity: 0;
        }
        50% {
            transform: translateX(10%);
            opacity: 1;
        }
    }

    /* Content */
    .cta__inner {
        position: relative;
        z-index: 1;
    }

    .cta__label {
        font-size: 0.75rem;
        font-weight: 500;
        letter-spacing: 0.25em;
        text-transform: uppercase;
        color: var(--gold-400);
        margin-bottom: 1.5rem;
    }

    .cta__title {
        font-family: var(--font-display);
        font-size: clamp(2.5rem, 6vw, 4.5rem);
        font-weight: 700;
        color: var(--white);
        line-height: 1.1;
        margin-bottom: 1.5rem;
    }

    .cta__text {
        font-size: clamp(0.95rem, 1.8vw, 1.1rem);
        color: var(--silver-200);
        max-width: 500px;
        margin: 0 auto 2.5rem;
        line-height: 1.8;
        font-weight: 300;
    }

    .cta__actions {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 1rem;
        flex-wrap: wrap;
        margin-bottom: 2rem;
    }

    .cta__btn {
        display: inline-flex;
        align-items: center;
        gap: 0.6rem;
        padding: 1rem 2.5rem;
        font-size: 0.85rem;
        font-weight: 600;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        border-radius: 50px;
        transition: all var(--transition-base);
    }

    .cta__btn--primary {
        color: var(--black);
        background: linear-gradient(
            135deg,
            var(--gold-300) 0%,
            var(--gold-400) 100%
        );
        box-shadow: 0 4px 30px rgba(212, 175, 55, 0.3);
    }

    .cta__btn--primary:hover {
        transform: translateY(-3px) scale(1.02);
        box-shadow: 0 10px 45px rgba(212, 175, 55, 0.45);
    }

    .cta__btn--secondary {
        color: var(--silver-100);
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.2);
        backdrop-filter: blur(8px);
    }

    .cta__btn--secondary:hover {
        background: rgba(255, 255, 255, 0.1);
        border-color: rgba(255, 255, 255, 0.35);
        transform: translateY(-3px);
    }

    .cta__disclaimer {
        font-size: 0.72rem;
        letter-spacing: 0.1em;
        color: var(--silver-400);
        font-weight: 300;
    }

    @media (max-width: 600px) {
        .cta__actions {
            flex-direction: column;
        }
        .cta__btn {
            width: 100%;
            max-width: 300px;
            justify-content: center;
        }
    }
</style>
